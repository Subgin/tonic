<p>
  <span>From (<%= min %>)</span>
  <%= input_tag(:range, min: min, max: max, onchange: "filter.range(this.value, '#{attribute}', 'a')") %>
</p>

<p>
  <span>To (<%= max %>)</span>
  <%= input_tag(:range, min: min, max: max, onchange: "filter.range(this.value, '#{attribute}', 'b')") %>
</p>
