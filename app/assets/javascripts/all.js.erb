//= require_tree ./app

window.map = {};
// window.types = JSON.parse('<%= data.config.filters.type.to_json %>');
window.collection = JSON.parse('<%= data.collection.to_json %>');

window.state.reset();
